[package]
name = "huml_parser"
version = "0.1.0"
edition = "2021"
description = "Rust wrapper for HUML parser (calls native OCaml executable)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tracing = "0.1"
