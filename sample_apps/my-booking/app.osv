app "my-booking" version "2.0.0" {
  role owner can share, delegate, accept_publish;
  role node can relay, share, accept_publish;
  role customer;

  ui_app "Service Provider" {
    allow owner;
  }

  ui_app "Service Customer" {
    allow customer, owner, node;
  }
}
